cmake_minimum_required(VERSION 2.8)
project(salvaimmagini)
find_package(OpenCV REQUIRED)
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable(salvaimmagini ./src/salvaimmagini.cpp)
message("${OpenCV_LIBS}")
target_link_libraries(salvaimmagini ${OpenCV_LIBS})
